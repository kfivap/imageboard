import React from 'react';
import {observer} from "mobx-react-lite";

const ReplyPreviewModal = observer(({preview}) => {
    console.log(preview)
    return (
        <div>

        </div>
    );
})

export default ReplyPreviewModal;